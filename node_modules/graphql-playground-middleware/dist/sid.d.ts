import * as lambda from 'aws-lambda';
export interface LambdaPlaygroundMiddleware {
    (event: any, context: lambda.Context, callback: lambda.Callback): void;
}
export interface Register {
    (options: any): LambdaPlaygroundMiddleware;
}
declare const gqlambda: Register;
export default gqlambda;
